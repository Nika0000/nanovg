# NanoVG Vulkan starter

This starter demonstrates GLFW surface creation, Vulkan dynamic rendering,
synchronization, swap-chain recreation, NanoVG drawing, and presentation.

Install a Vulkan SDK, then build with:

```sh
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release
```

